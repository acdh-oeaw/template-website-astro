services:
  email:
    image: axllent/mailpit:latest
    container_name: template-website-astro-email
    restart: unless-stopped
    environment:
      MP_DATABASE: /data/mailpit.db
      MP_ENABLE_CHAOS: true
    ports:
      - "${MAILPIT_API_PORT:-8025}:8025"
      - "${EMAIL_SMTP_PORT:-1025}:1025"
    volumes:
      - email-data:/data

  minio:
    image: minio/minio:latest
    container_name: template-website-astro-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY}
    command: server /data --console-address ":9001"
    ports:
      - "${S3_PORT:-9000}:9000"
      - "${MINIO_WEB_UI_PORT:-9001}:9001"
    volumes:
      - minio-data:/data

  imgproxy:
    image: ghcr.io/imgproxy/imgproxy:latest
    container_name: template-website-astro-imgproxy
    restart: unless-stopped
    depends_on:
      - minio
    environment:
      # IMGPROXY_MALLOC: jemalloc
      IMGPROXY_KEY: ${IMGPROXY_KEY}
      IMGPROXY_SALT: ${IMGPROXY_SALT}
      IMGPROXY_USE_S3: true
      IMGPROXY_ALLOWED_SOURCES: s3://
      IMGPROXY_S3_ENDPOINT: http://minio:${S3_PORT:-9000}
      AWS_ACCESS_KEY_ID: ${S3_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${S3_SECRET_KEY}
      AWS_EC2_METADATA_DISABLED: true
    ports:
      - "${IMGPROXY_PORT:-8080}:8080"

volumes:
  email-data:
    name: template-website-astro-email-data
  minio-data:
    name: template-website-astro-minio-data
