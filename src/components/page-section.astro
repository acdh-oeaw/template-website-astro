---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<section class:list={["mx-auto grid w-full max-w-screen-lg gap-y-6 px-4 py-6", className]}>
	<slot />
</section>
