---
interface Props {
	class?: string;
	id: string;
}

const { class: className, id } = Astro.props;
---

<main class:list={["outline-hidden", className]} data-pagefind-body id={id} tabindex={-1}>
	<slot />
</main>
