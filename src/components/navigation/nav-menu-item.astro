---
import Link from "@/components/link.astro";

interface Props {
	class?: string;
	href: URL | string;
}

const { class: className, ...rest } = Astro.props;
---

<Link
	{...rest}
	class:list={[
		"inline-flex w-full rounded px-4 py-2.5 transition hover:bg-neutral-50 hover:text-neutral-950 dark:hover:text-neutral-0",
		className,
	]}
	data-nav-menu-item
>
	<slot />
</Link>
