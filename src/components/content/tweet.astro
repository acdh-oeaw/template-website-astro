---
import StaticTweet from "astro-tweet";

interface Props {
	id: string;
}

const { id } = Astro.props;
---

<figure>
	<StaticTweet id={id} />
	{
		Astro.slots.has("default") ? (
			<figcaption>
				<slot />
			</figcaption>
		) : null
	}
</figure>
