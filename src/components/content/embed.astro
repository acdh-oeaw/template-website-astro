---
interface Props {
	src: string;
	/** Added by `@acdh-oeaw/mdx-lib#with-iframe-titles`. */
	title?: string;
}

const { src, title } = Astro.props;
---

<figure>
	<iframe
		allowfullscreen
		class="aspect-video w-full overflow-hidden rounded-1"
		src={src}
		title={title ?? "Iframe"}></iframe>
	{
		Astro.slots.has("default") ? (
			<figcaption>
				<slot />
			</figcaption>
		) : null
	}
</figure>
